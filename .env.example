# Documentation only. Server and client examples live in their workspace directories.
# Never place secrets in VITE_* variables.
# Step 3 requires server-only MONGODB_URI. Configure it privately using the server example and Atlas guide.
# No root environment file is loaded by the application.

