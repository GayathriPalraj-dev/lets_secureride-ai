NODE_ENV=development
PORT=5000
CLIENT_ORIGIN=http://localhost:5173
LOG_LEVEL=info

# Future steps only; not validated or used by Step 2.
MONGODB_URI=replace_later
JWT_ACCESS_SECRET=replace_later
JWT_REFRESH_SECRET=replace_later
STRIPE_SECRET_KEY=replace_later
STRIPE_WEBHOOK_SECRET=replace_later
AWS_REGION=ap-south-1
AWS_S3_MEDIA_BUCKET=replace_later

